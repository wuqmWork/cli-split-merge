# 审查报告修复记录（2026-09-16）

本轮以用户提供的《CLI Split & Merge 项目审查报告》为基线，对 merge-v5 代码进行修复。

## 已修复

### 阻断
- B1：删除 `HatchSegment` 类型别名上的非法 `#[derive(Debug)]`。
- B2：补齐 `src-tauri/icons/`，包含 Windows `icon.ico` 与 PNG 图标；`tauri.conf.json` 显式配置 bundle icons。

### 高优先级
- H1：
  - 合并：重写 `$$LAYERS/` 为合并后的层并集数量；`$$DIMENSION/` 使用各输入头部包围盒的并集。
  - 分割：不再复制原全局 XY `$$DIMENSION/`；按对应振镜裁剪区域经过 C3→局部变换后的保守包围盒重写 XY，Z 沿用输入头部。
- H2：`$$UNITS/1` 与 `$$UNITS/1.0` 解析为数值后按容差比较；缺失按 1.0。
- H3：接缝采用唯一所有权规则：共享竖边归右侧区域，共享横边归上侧区域；穿越接缝仍分别保留两侧片段。
- H4：HATCHES 两种格式按精确字段数量判别，歧义/非法格式返回明确错误。
- H5：分割当前文件失败时统一清理 `.cli.part`；错误信息明确已完成文件数。finalize 阶段也清理尚未 rename 的临时文件。
- H6：split 输入改为字节 `read_until`，文本采用 lossy 解码，GBK/非法 UTF-8 的 LABEL/注释不再导致整单读取失败；几何数值仍按 ASCII 解析。

### 中优先级
- M1：POLYLINE/HATCHES 单实体上限从 1 亿降低到 100 万，避免损坏文件触发 GB 级预分配。
- M2：未知几何行不再静默丢弃，改为透传到输出。
- M3：前端与后端都对重复输入路径去重/拒绝。
- M5：Excel 参数加载命令改为 `spawn_blocking`，避免阻塞 UI。
- M6：恢复参数快照时重新检查源坐标系、有限数值和 `a²+b²≈1`。
- M7：增加后端全局任务互斥，防止分割/合并并发争用临时文件。（取消令牌未加入本轮 UI。）
- M8：Rust split 默认写缓冲改为 4 MB，与前端及性能文档一致。
- M9：split/merge 都检查 `.cli` 扩展名；merge 现在要求 `GEOMETRYSTART/GEOMETRYEND` 成对存在。
- M10：split 在启动前计算目标路径并阻止输出覆盖任何输入文件。

### 低优先级/工程卫生
- L1：尚未实现的并行线程、日志、缓存清理、Logo/启动图与深色主题均禁用并标注“即将上线”；关于页版本通过 Tauri API 读取，不再硬编码。
- L2：补充基础 CSP。
- L3：新增 `.gitignore`，移除交付目录中的 tsbuildinfo 与生成的 Vite JS/d.ts；同步修正文档中“合并未实现”的矛盾描述。
- L5：删除无效的 `drop(outputs.iter_mut()...)`；合并进度的 completed 改为“已遇到过的输入文件数”。

## 有意保留 / 待确认

- M4：没有加入“排序”按钮。此前 UI 需求明确要求删除排序控件；当前同层顺序仍按文件选择数组顺序。若后续需要绝对可控顺序，建议使用文件卡片拖拽，而不是恢复单独的“排序”按钮。
- M7：本轮只实现后端互斥，未增加取消任务按钮/取消令牌。
- L6：仍固定生成 24 路输出，保留设备 24 路齐套语义。
- 分割 `$$DIMENSION/` 使用振镜区域的局部保守包围盒，不额外做第二遍扫描求“实际几何最小包围盒”，以避免明显降低分割性能。
