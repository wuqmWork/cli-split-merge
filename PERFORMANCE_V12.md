# v12 CLI 分割性能优化

## 根据实机资源占用做的调整

用户截图中 CPU 约 17%，NVMe C: 活动时间约 1%，NVMe D: 约 11%。这说明当前任务并没有把磁盘吃满，单个大 CLI 时更像是单核解析/裁剪/格式化成为主要瓶颈。

## 本版修改

1. **单文件大 HATCHES 并行裁剪**
   - 以前的并行仅针对“多个输入 CLI”；只有一个 CLI 时，实际上只能使用一个工作线程。
   - v12 在 `parallel=true` 且只有一个输入 CLI 时，允许大 HATCHES 使用多个 CPU 线程。
   - 仅当单条 HATCHES 线段数 >= 4096 时启用，避免小任务线程开销反而拖慢。
   - 按 1024 段/chunk 并行执行候选区域筛选、Liang-Barsky 裁剪和 C3→振镜局部变换。
   - chunk 结果按原顺序合并，保持输出线段顺序稳定。

2. **坐标输出零 String 分配**
   - 旧版 `write_cli_number()` 每写一个 X/Y 坐标都会构造一个新的 `String`。
   - v12 改为 `ryu::Buffer` 栈上格式化并直接写入 `BufWriter`。
   - 保持原来的 1e-6 取整、去除 `-0`、去除 `.0` 规则。
   - 对大 HATCHES 文件，这是高频热路径优化。

3. **线程数不再被输入文件数量限制**
   - 旧逻辑 `workers.clamp(1, total)` 导致只有 1 个输入文件时线程数永远为 1。
   - v12 改为按 CPU 可用并行度限制；单文件可用于内部 HATCHES 并行。

## 建议设置

- 并行处理：开启
- 线程数：0（自动）或 4
- 读缓冲：16 MB
- 写缓冲：4 MB

如果单个 HATCHES 行本身很短但文件里有大量 HATCHES 行，下一步应做“几何记录批处理流水线”，把多行几何批量并行解析/裁剪后按序写出。
