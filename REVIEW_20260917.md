# 0.4（v13-max-performance）审查与修复报告

日期：2026-09-17
依据：`PERFORMANCE_V13_MAX.md` 优化说明 + 0.3 全部修复清单逐项回归核对

## 一、结论

v13 的 zero-copy / mmap 性能优化实现正确，但 **0.4 目录是从旧代码复制出来的，丢失了 0.3 全部 5 项修复**。本轮已全部移植回并实机验证；另外清理了 v13 遗留的 4 个编译警告。

## 二、0.3 修复回归核对（0.4 初版状态 → 本轮处置）

| # | 修复项 | 0.4 初版 | 处置 |
| --- | --- | --- | --- |
| 1 | `main.rs` Windows GUI 子系统声明（启动不弹控制台） | ❌ 丢失 | ✅ 已补回 |
| 2 | productName 去 `&`（"CLI Split & Merge" 会使 WiX main.wxs 非法 XML，MSI 打包必败） | ❌ 丢失 | ✅ 已改回 "CLI Split and Merge" |
| 3 | `parameter.rs` 精简参数格式兼容（变化参数.xlsx：缺 类别/变换名称/旋转角deg/m11~m33 列时按精简格式解析，旋转角=atan2(b,a)，矩阵=[[a,-b,tx],[b,a,ty],[0,0,1]]） | ❌ 丢失 | ✅ 已移植 |
| 4 | Excel filter 放宽 xlsx/xls/xlsm/xlsb | ❌ 丢失 | ✅ 已放宽 |
| 5 | App.tsx split 进度 `Math.max` 单调防护（与 merge 侧对齐） | ❌ 丢失 | ✅ 已补回 |

merge.rs 的 0.2/0.3 修复项（fmt_num strip `.0`、进度节流、`use std::time`）在 0.4 中在位，无需处理。

## 三、v13 性能优化审查（split.rs 重构）

| 项 | 评估 |
| --- | --- |
| mmap（`memmap2`）零拷贝读取 | ✅ 正确；几何区按原始字节扫描行首/行尾，不构造 String |
| 行定位扫描 | ✅ `find_newlines` 一次性收集换行偏移，扫描逻辑与旧实现语义一致 |
| `lexical_core` 字节级数值解析 | ✅ 热路径无分配；ASCII 假定对 CLI 格式成立 |
| 进度上报 | ✅ 保留 250ms 节流 |
| 并行任务 | ✅ 沿用 v11 的命名冲突防护（启动前目标路径去重） |
| 潜在注意点 | mmap 输入文件在写入期间被外部截断会触发 SIGBUS 类问题——Windows 下 `virtual_alloc` 失败会正常报错退出，风险可接受，暂不改 |

## 四、本轮额外清理

- 删除 v13 遗留死代码：旧 String 版 `parse_i32` / `parse_usize`（未再引用）
- 删除未使用的 `bytes_read` 变量及其赋值链（进度百分比改由 `line_start` 直接推导）

## 五、实机验证

| 验证 | 结果 |
| --- | --- |
| `cargo test`（首轮，移植修复后） | 18/18 通过，4 个警告 |
| `cargo test`（清理警告后） | **18/18 通过，无警告** ✅ |
| `npm install` + `npm run build`（tsc -b + vite） | ✅ 通过 |
| `tauri build` 打包 | ✅ MSI + NSIS 双包生成（product name 修复后 MSI 正常出包） |

产物（2026-09-17 00:12）：
- `src-tauri\target\release\bundle\msi\CLI Split and Merge_1.0.0_x64_en-US.msi`（2.52 MB）
- `src-tauri\target\release\bundle\nsis\CLI Split and Merge_1.0.0_x64-setup.exe`（1.76 MB）

## 六、补充修复：分割进度条与实际不符（2026-09-17 00:57）

用户反馈“分割很快，但进度条不匹配”。根因两条：

| # | 问题 | 修复 |
| --- | --- | --- |
| 1 | `split_one_file` 用 `index - 1` 推算“已完成文件数”。并行模式下各 worker 处理顺序与序号无关，第 8 个文件一开始就会报 `completed=7`（87.5%），而实际可能一个都没完成 → 进度瞬间虚高后卡死 | 新增 `ProgressTracker`（`AtomicUsize` 完成数 + 每文件 `AtomicU32` 定点百分比槽位），上报统一读共享状态 |
| 2 | 每个 worker 只报自己那一个文件的进度：N 个文件并行各到 50% 时，整体被算成 `0.5/N`（8 文件=6.25%）→ 进度条严重落后，最后一刻才跳到 100% | `overall_percent` 聚合所有在跑文件的贡献：`(已完成数 + Σ各文件百分比/100) / 总数 × 100` |
| 3 | 250ms 节流 + 快速任务几乎不出中间帧；且循环最后一段被节流吞掉，进度停在 90% 多 | 节流降到 60ms（`PROGRESS_THROTTLE`）；几何阶段结束补一次该文件的 100% 上报 |

配套改动：`finish_file` 先清槽位再递增完成数，避免重复计数；新增 3 个单测锁定聚合语义（并行 8×50%=50%、完成后不重复计、单文件等价）。`cargo test` 21/21 通过、无警告，已重新打包。

## 七、建议

1. **建立版本基线**：0.4 从旧快照复制导致 0.3 修复全丢，建议 0.4 之后的版本从 0.3 目录复制，或直接把 0.3→0.4 的改动用 git 管理，避免逐版本手工搬运。
2. GUI 子系统、productName、精简参数格式、Excel filter、进度防护这 5 项已写回 0.4，后续版本复制时请以 0.4 当前状态为基线。
